# DTUI-1 — Detail-pane key (collision with drop)

- **`d` stays as drop** (unchanged from today). `,` is the new detail-pane toggle.
- Reads as "aside / pause" — fits a pane that is an aside to the main flow.
