"""SQLite index — the derived store.

Source of truth is always the filesystem. The index is rebuildable via
`octopus reindex`. See `.spectacular/specs/SCHEMA-INDEX.md` for
the authoritative schema.
"""
